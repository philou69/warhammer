app_home:
    path: /
    defaults: { _controller: "AppBundle:App:index"}
    methods: ['GET']

app_battle:
    resource: 'routing/battle.yml'
    prefix: /battle

app_army:
    resource: 'routing/army.yml'
    prefix: /army

app_unit_army:
    resource: 'routing/unit.army.yml'
    prefix: /unitarmy

app_figurine_army:
    prefix: /figurine-army
    resource: routing/figurine.army.yml

app_resume:
    resource: 'routing/resume.yml'
    prefix: /resume

app_user:
    resource: 'routing/user.yml'
    prefix: /user

app_race:
    resource: 'routing/race.yml'
    prefix: /race

app_unit:
    resource: 'routing/unit.yml'
    prefix: /unit

