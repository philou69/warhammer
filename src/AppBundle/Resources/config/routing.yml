app_home:
    path: /
    defaults: { _controller: "AppBundle:App:index"}

app_battle:
    resource: 'routing/battle.yml'
    prefix: /battle

app_army:
    resource: 'routing/army.yml'
    prefix: /army

app_figurine:
    resource: 'routing/figurine.yml'
    prefix: /figurine

app_photo_figurine:
    resource: 'routing/photo.figurine.yml'
    prefix: /photo/figurine/

app_photo_user:
    resource: 'routing/photo.user.yml'
    prefix: /photo/user

app_resume:
    resource: 'routing/resume.yml'
    prefix: /resume

app_user:
    resource: 'routing/user.yml'
    prefix: /user