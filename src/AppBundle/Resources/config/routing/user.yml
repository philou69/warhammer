user:
    path: /view
    defaults:
        _controller: "AppBundle:User:view"

user_edit:
    path: /{id}/edit
    defaults:
        _controller: "AppBundle:User:edit"
    requirements:
      id: \d+

