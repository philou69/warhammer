{% extends 'AppBundle::layout.html.twig' %}

{% block title %}{{ race.name }}{% endblock %}

{% block body %}
    <h1 class="text-center">{{ race.name }}</h1>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Nombre d'equipements</th>
            </tr>
        </thead>
        <tbody>
        {% for figurine in race.figurines %}
            <tr class="table-hover">
                <td><a href="{{ path('figurine_view', {'id': figurine.id}) }}">{{ figurine.name }}</a></td>
                <td>{{ figurine.equipements | length }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}